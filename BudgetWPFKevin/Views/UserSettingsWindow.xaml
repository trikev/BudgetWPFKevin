<Window x:Class="BudgetWPFKevin.Views.UserSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Användarinställningar"
        Height="500"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Ange din årsinkomst och arbetstid"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Yearly Income -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Årsinkomst (kr):"/>
            <TextBox Text="{Binding YearlyIncome, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Yearly Work Hours -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Årsarbetstid (timmar):"/>
            <TextBox Text="{Binding YearlyWorkHours, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="(Standard: 1920 timmar = 40h/vecka * 48 veckor)"
                       FontSize="11"
                       Foreground="Gray"
                       Margin="0,-10,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Skatt:"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
         Text="{Binding TaxRate, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
         IsEnabled="{Binding IsEditing}"/>

                <Button Grid.Column="1" 
Content="Ändra" 
Command="{Binding ToggleEditCommand}"
Width="50"
Height="36"
FontSize="10"
Padding="2"
Margin="5,0,0,15"
                />
            </Grid>
        </StackPanel>

        <!-- Validation Error -->
        <TextBlock Grid.Row="3"
                   Text="{Binding ValidationError}"
                   Foreground="Red"
                   FontSize="12"
                   Visibility="{Binding ValidationError, Converter={StaticResource StringToVisibilityConverter}}"
                   Margin="0,0,0,10"/>

        <!-- Info Box -->
        <Border Grid.Row="4"
                Background="#E3F2FD"
                BorderBrush="#2196F3"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="ℹ️ Information" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock TextWrapping="Wrap" FontSize="12">
                    Dessa uppgifter används för att beräkna din månadsinkomst och effekten av frånvaro (VAB/sjuk).
                    Timlön beräknas som: Årsinkomst ÷ Årsarbetstid.
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Spara"
                    Command="{Binding SaveCommand}"
                    IsDefault="True"/>
            <Button Content="Avbryt"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>