<UserControl x:Class="BudgetWPFKevin.Views.TransactionListsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BudgetWPFKevin.Views">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:IncomeListView Grid.Column="0" 
                              Margin="10"
                              DataContext="{Binding RegularTransactions}"/>

        <local:ExpenseListView Grid.Column="1" 
                               Margin="10"
                               DataContext="{Binding RegularTransactions}"/>

        <local:RecurringTransactionsListView Grid.Column="2" 
                                             Margin="10"
                                             DataContext="{Binding RecurringTransactions}"/>
    </Grid>
</UserControl>