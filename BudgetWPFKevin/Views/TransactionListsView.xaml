<UserControl x:Class="BudgetWPFKevin.Views.TransactionListsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BudgetWPFKevin.Views">

    <!-- Layout med tre kolumner: Inkomster, Utgifter, Återkommande transaktioner -->
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:IncomeListView Grid.Column="0" 
                              Margin="10"
                              DataContext="{Binding IncomesListVM}"/>

        <local:ExpenseListView Grid.Column="1" 
                               Margin="10"
                               DataContext="{Binding ExpensesListVM}"/>

        <local:RecurringTransactionsListView Grid.Column="2" 
                                             Margin="10"
                                             DataContext="{Binding RecurringTransactionsListVM}"/>
    </Grid>
</UserControl>